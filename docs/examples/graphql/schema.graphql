type Item {
  id: ID!
  name: String!
  price: Float!
  qty: Int!
}

type Query {
  items: [Item!]!
  item(id: ID!): Item
}

type Mutation {
  addItem(name: String!, price: Float!, qty: Int!): Item!
}
